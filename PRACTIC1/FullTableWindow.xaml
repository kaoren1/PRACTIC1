<Window x:Class="PRACTIC1.FullTableWindow"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PRACTIC1"
        mc:Ignorable="d"
        Title="FullTableWindow" Height="450" Width="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" x:Name="FTable" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер" Visibility="Collapsed" Binding="{Binding ID_Employee}" />
                <DataGridTextColumn Header="Имя" Binding="{Binding Name1}" />
                <DataGridTextColumn Header="Фамилия" Binding="{Binding Surname}" />
                <DataGridTextColumn Header="Должность" Binding="{Binding Position}" />
                <DataGridTextColumn Header="Название проекта" Binding="{Binding Title}" />
                <DataGridTextColumn Header="Описание проекта" Binding="{Binding Dedcription}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
